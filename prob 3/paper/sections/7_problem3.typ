= 问题三：机器狗搜索、定位与清除

== 问题重述与总体思路

在半径 $R=1800$ m 的圆形区域内，存在10至16个频道互异的全向干扰源。每个源的有效接收半径未知但位于1000至1500 m之间，测向误差不超过 $plus.minus 1 degree$。机器狗以5 m/s移动，完成换频、检测、光学定位和激光清除分别需要1 s、5 s、3 s和2 s。问题要求在确保全部清除的前提下尽量缩短完成时间。

为同时满足完整性与效率，本文建立“完备覆盖普查—鲁棒方向交会—滚动逼近清除—动态路线调度”的闭环策略。七个基础扫描点保证不漏检；方向楔形交集给出源的保守可行域；只有满足20 m清除范围的充分条件时才执行清除；扫描、验证和清除任务由滚动路线规划统一安排。

== 模型假设与符号

假设干扰源在任务期间固定，一个频道至多对应一个源；接口返回的方向读数满足题设误差界；机器狗可在圆域内沿直线移动。接收半径的具体数值不作估计，覆盖与定位均按1000 m下界进行保守设计。

#figure(
  table(
    columns: (1.2fr, 3.4fr),
    align: (center, left),
    inset: 5pt,
    stroke: 0.5pt,
    [符号], [含义],
    [$cal(A)$], [半径1800 m的圆形任务区域],
    [$cal(S)$], [基础扫描点集合],
    [$p_i, hat(theta)_i$], [第 $i$ 次测量的位置与示向读数],
    [$P_k$], [$k$ 次示向后的源位置可行域],
    [$c_k, u_k$], [可行域质心及质心到最远顶点的距离],
    [$L, T$], [总移动路程与总虚拟时间],
  ),
  caption: [主要符号说明],
)

若频道切换、检测、光学定位和清除次数分别为 $n_s,n_m,n_o,n_c$，则总时间为

$ T = L/5 + n_s + 5 n_m + 3 n_o + 2 n_c. $

一次换频并检测耗时6 s，约等价于行走30 m，而行走1 km需200 s，因此路线长度是主要优化对象，但必要的观测不能为节省时间而省略。

== 七点完备搜索模型

取原点和半径 $d=1150$ m 正六边形的六个顶点为基础扫描点：

$ cal(S) = {(0,0)} union {1150(cos(k pi/3), sin(k pi/3)): k=0, dots, 5}. $

为证明连续圆域无漏检，只需考察相邻环点角平分线上的最坏位置。令该方向上的点到原点距离为 $t$，其与最近环点的夹角为 $phi=pi/6$，则到环点的距离为

$ rho_d(t) = sqrt(t^2+d^2-2 t d cos(phi)), $

而到全部七点的最近距离为 $g_d(t)=min(t,rho_d(t))$。两项在 $t_0=d/(2cos(phi))$ 处相等，因此覆盖半径为

$ G(d) = max(d/(2cos(phi)), sqrt(R^2+d^2-2 R d cos(phi))). $

代入 $R=1800$ m、$d=1150$ m，得 $t_0=663.95$ m，边界最差距离为988.45 m，故 $G(1150)=988.45<1000$ m。于是即使某源的接收半径恰取下限，也至少能在一个扫描点被发现。

若去掉中心点而只保留六个环点，最优对称半径约为

$ d^* = R/(2cos(pi/6)) = 1039.23 " m", $

其最坏距离仍超过1000 m。因此在所采用的正六边形对称结构下，七点是保守覆盖所需的最少点数。

== 鲁棒定位与滚动逼近

第 $i$ 次示向位置为 $p_i$，读数为 $hat(theta)_i$。为吸收角度输出的小数舍入，在题设 $1 degree$ 误差上增加 $0.005001 degree$ 数值余量，得到楔形半宽

$ epsilon = 1.005001 degree. $

以 $p_i$ 为顶点、$hat(theta)_i$ 为中心线、半宽为 $epsilon$ 的前向楔形记为 $W_i$，累计可行域为

$ P_k = cal(A) ∩ ⋂_(i=1)^k W_i. $

当某频道只有一次示向时，在首个观测点的局部坐标系中构造 $(764,526)$ m与 $(764,-526)$ m两个镜像候选点，选择插入剩余主路线后增量较小的一侧。为减少不必要移动，先在首站至标准点连线的10%位置试测；仅当无信号时才回到标准候选点补测。

获得不少于两次示向后，计算 $P_k$ 的面积质心 $c_k$，并定义保证半径

$ u_k = max_(v in "Vert"(P_k)) norm(c_k-v). $

若 $u_k <= 18$ m，则真源必位于以 $c_k$ 为心、半径18 m的圆内，在20 m清除半径内仍有2 m余量，可以执行光学定位与清除。这里不采用“可行域直径不超过40 m”作为判据，因为直径条件并不保证可行域被以质心为圆心的20 m圆覆盖。

若 $u_k>18$ m，则从当前位置 $x$ 向质心方向移动

$ s_k = max(25, 0.85 norm(x-c_k)) $

米并重新测量。新楔形不断收缩可行域，直到满足安全清除条件或获得 `near` 反馈。

== 动态路线调度与覆盖认证平移

待执行任务分为剩余扫描点、单示向频道的验证点以及多示向频道的定位清除点。设剩余扫描点数为 $m$，由于 $m<=6$，可精确枚举至多 $6!=720$ 条开放排列。随后把服务任务按最小插入增量

$ Delta(h;i) = norm(a-h)+norm(h-b)-norm(a-b) $

插入相邻节点 $a,b$ 之间，并枚举不同的下一任务作为多起点，用最近邻补全和开放式2-opt改进路线。系统每次只执行规划路线的第一个任务，获得新反馈后立即重算。

机器狗已经在某处停靠时，还会评估对其他频道补测的价值。一次换频检测的6 s成本折算为30 m行程；只有预计可行域缩减所节省的后续路程超过30 m时，才执行共享观测。

为进一步降低绕路，下一扫描点可向当前位置或服务点方向小幅平移。但平移后的扫描集合 $tilde(cal(S))$ 必须满足

$ cal(A) subset.eq union_(s in tilde(cal(S))) B(s,1000). $

本文使用保守四叉树证书检查整个连续圆域，而非有限网格抽样。若任一小区域不能证明完全处于场地外或被某个1000 m圆完整覆盖，就拒绝平移并回退到原扫描点。

== 清除确认、恢复与终止

每次清除成功后在原地复测该频道，只有返回无信号才登记为已清除，形成“定位—清除—确认”闭环。若清除异常未命中，则返回标准第二检测点重新观测，最多恢复两次。

通常只有在覆盖任务完成且所有已发现频道均清除并复测无信号后才能终止。题设给出源数上限16，因此当16个不同频道均已成功清除时可提前结束；仅发现16个但尚未全部清除时不能停止。

== 实验结果

为检验效率，在200个相同随机场景上对各策略逐局配对，所有方案均清除2615/2615个源。

#figure(
  table(
    columns: (2.2fr, 1.4fr, 1.2fr),
    align: (left, right, center),
    inset: 5pt,
    stroke: 0.5pt,
    [方案], [平均虚拟时间/s], [清除结果],
    [原基线], [3473.244274], [2615/2615],
    [稳健多起点], [3237.772791], [2615/2615],
    [增加16源停止], [3237.314681], [2615/2615],
    [最终策略], [3223.446481], [2615/2615],
  ),
  caption: [200局同种子配对实验结果],
)

最终策略相对原基线平均减少249.797793 s，即下降7.192059%；200局中185局更快、15局更慢，平均配对差的近似95%区间为 $[-276.133641,-223.461946]$ s。在接收半径全部取1000 m的50局压力测试中，最终策略清除667/667个源，平均时间3259.54 s。

在五次真实接口演练中，共清除71/71个源，单局平均总时间3354.051945 s，按全部71个源加权的平均定位清除时间为236.200841 s/源。所有清除均一次命中，无恢复、失败或放弃。

#figure(
  table(
    columns: (0.7fr, 0.7fr, 0.9fr, 1.25fr, 1.55fr),
    align: (center, center, center, right, right),
    inset: 4pt,
    stroke: 0.5pt,
    [局次], [源数], [清除], [总时间/s], [平均时间/(s/源)],
    [1], [13], [13/13], [3438.077299], [264.467485],
    [2], [16], [16/16], [3317.776749], [207.361047],
    [3], [13], [13/13], [3322.917953], [255.609073],
    [4], [14], [14/14], [3357.875787], [239.848270],
    [5], [15], [15/15], [3333.611937], [222.240796],
  ),
  caption: [第二轮演练结果],
)

不同演练局的源分布不同，故单局耗时差异只用于描述真实运行表现，不能用于证明某个模块的因果提速；提速结论以同种子配对实验为依据。

== 正式测试记录

#figure(
  table(
    columns: (0.9fr, 0.8fr, 0.8fr, 1fr, 1.1fr, 1.7fr),
    align: center,
    inset: 4pt,
    stroke: 0.5pt,
    [测试], [源数], [清除数], [清除比例], [总时间/s], [平均定位清除时间],
    [第1次], [待测], [待测], [待测], [待测], [待测],
    [第2次], [待测], [待测], [待测], [待测], [待测],
    [第3次], [待测], [待测], [待测], [待测], [待测],
  ),
  caption: [三次正式测试结果待填表],
)

正式提交时，以三次正式测试数据替换表中“待测”项，演练结果不代替题目要求的正式测试结果。

== 模型评价

模型的主要优点是完整性与优化层解耦：固定七点和清除复测提供可验证的正确性底座，路线插入、共享观测与认证平移仅在不破坏底座时缩短路程。模型不足在于路线部分仍属滚动启发式，不能保证全局最短；第二检测点与逼近系数为仿真筛选参数，其效率可能随真实误差分布改变。此外，连续覆盖认证虽保守可靠，但会拒绝一部分实际安全、却难以在给定细分深度下证明的平移方案。
