# 问题4工程进度

> 最后更新：2026-09-12（第二轮时间优化收尾 + drill12 首局真机）
> 当前阶段：**默认策略已升级为 23 点包围网（中心 + 内环 8×980 + 外环 14×1848）+ 验证偏置 −700 m**，
> 64 项测试全过，drill12 首局真机演练 10/10 全清；历史 27 点策略完成 10 局成功真实演练（120/120）。

## 1. 当前结论

问题 4 当前策略为「**23 点包围网覆盖普查 + 滚动最近邻调度 + 验证任务适度延后 + 沿示向度的同向推进 +
信号消失二分恢复 + 光学环形兜底清除 + 清除后复测**」。离线自验模拟器（含定向源）上
1100 局默认策略回归全部清除。经第二轮严格布局搜索与配对实验，默认值由「25 点 + 偏置 −300 m」
升级为「**23 点 + 偏置 −700 m**」（内环 8×980 m + 外环 14×1848 m）：相对 25 点 + −300 m
配对 300 案例平均再省 **6.21%**（−479.6 s，SE 35.4），300/300 全清，step=1 m 细网格覆盖余量 15.4 m。
历史 27 点策略的 10 局正常真实演练累计清除 120/120；**drill12 为升默认后首局真机，10/10 全清**。
正式测试尚未执行。

三组 25 点独立归档（上一版基准，保留备查），均使用外环 16 点、半径 1840 m、验证偏置 −300 m：

| 归档 | 案例数 | 干扰源 | 全部清除案例 | 平均总时间 |
|---|---:|---:|---:|---:|
| `outputs/optimized25_p0_300` | 300 | 3937/3937 | 300/300 | 6977.9 s |
| `outputs/optimized25_p05_500` | 500 | 6409/6409 | 500/500 | 7696.1 s |
| `outputs/optimized25_p1_300` | 300 | 3923/3923 | 300/300 | 8354.8 s |

合计1100局、14269个干扰源全部清除（25 点版本）。三个目录均保留独立的配置、逐案例记录与汇总，
没有覆盖 `outputs/optimized_*` 下的27点调度优化结果。
**注意**：25 点 + 偏置 −300 m 已被 23 点 + 偏置 −700 m 取代；上述归档仅作历史对照。

## 2. 已完成工作

| 模块 | 状态 | 说明 |
|---|:---:|---|
| 题面与协议解析 | ✅ | 复用问题 3 的四接口；补充定向源覆盖规则 |
| **包围网覆盖搜索** | ✅ | 中心 + 内环 8×980 + 外环 14×1848 = **23 点**；step=1 m 细网格最坏 984.6 m、余量 15.4 m |
| 覆盖性数值校核 | ✅ | 位置网格 × 朝向采样，零漏检；细采样复核 |
| 同向推进点 | ✅ | 沿示向度朝源推进 300 m（取代问题 2 垂直候选点） |
| **定向源定位** | ✅ | 信号消失二分恢复 + 环形兜底（/clear 忽略覆盖角） |
| 定向源离线模拟器 | ✅ | 楔形覆盖判定、/clear 忽略覆盖角、定向源随机生成 |
| 冻结重试机制 | ✅ | 清除失败冻结频道、等新观测再重试，避免用带偏的观测反复重试 |
| 真实模拟器连接 | ✅ | 复用问题 3 客户端/后端（串行、幂等、墙钟看门狗） |
| 演练真值读取 | ✅ | `practice-p4-*.result.json` 读总数与定向数 |
| 日志体系 | ✅ | 每局 http.jsonl / summary.json / effective_config.json + 全局索引 |
| 自动测试 | ✅ | 64 项全部通过 |

## 3. 关键设计决策与依据

### 3.1 为什么扫描点从 7 点变成 23 点

问题 3 的七点覆盖对**全向源**成立（离得近就能测到）。定向源只在 180° 覆盖楔内可测，
"离得近"不再保证"测得到"。数值搜索表明：更稀疏的布局（如 19 点中心+内环6+外环12、
中心+内环6×1000+外环18×1850）存在漏检空隙。

第一轮搜索采用中心+内环8×980+外环16×1840（25 点），当时的判据是粗网格（step=40 m），
并据此认为"23 点存在盲区、24 点余量过薄"。**第二轮改用全局细网格（step=1 m）复核，发现
粗网格会低估最坏所需半径最多约 42 m**，该结论不成立。严格重搜后：

- **23 点（中心+内环8×980+外环14×1848）**：step=1 m 最坏 984.6 m，余量 **15.4 m**，可覆盖；
- 22 点（中心+内环8×990+外环13×1854）：余量 10.8 m，可覆盖但更薄；
- 24 点（内环8×960+外环16×1850）：余量仅 4.8 m，反而更薄，已淘汰；
- 19/21 点等更稀疏布局存在确定盲区。

当前采用 **23 点**为默认：仍有 15.4 m 余量，巡回长度比 25 点短，配对实验更省时。
这不构成全局最少点数的证明。

### 3.2 为什么外环必须布到区域外（1848 > 1800）

贴边且楔朝外的定向源，其覆盖楔完全落在区域外侧；内接于圆盘的多边形永远盖不住圆盘
边缘，只有把扫描点布到区域外才能包围住这类源。机器狗允许提交区域外坐标，合法。

### 3.3 为什么信号消失不判失败

定向源楔外不可测。逼近途中测到 `no_signal`，绝大多数情况是**越过了源**（走出覆盖楔），
而非源消失。若沿用问题 3 的"no_signal 即失败"逻辑，会把大量定向源漏掉。改为在
[最后有信号点, 无信号点] 之间二分恢复。

### 3.4 为什么需要"冻结重试"

楔形边缘的定向源，其第一次探测点可能恰在楔边界上，沿示向度推进会走出楔，二分搜索
的"单调性假设"失效。若在同一批（可能带偏的）观测上反复重试，只会耗光次数。冻结该
频道、等后续扫描点从更好角度补观测后再重试，可把这类案例救回来（实测 seed=1038/1045
靠此从漏检变为全清）。

## 4. 待办（按优先级）

- [x] 64 项自动测试重跑通过；默认值已切至 **23 点 + 偏置 −700 m**（`config.py` + `configs/baseline.json`）。
- [x] **23 点新策略首局真机（drill12）**：10/10 全清、`all_done`；`effective_config.json`
      确认为内环 8×980 + 外环 14×1848（23 点）、`task_bias_verify_m=-700.0`。
- [ ] **继续 23 点真机演练**（drill13、drill14…）：每局需在模拟器点「问题 4 演练测试」开新会话，
      核对全清与 `effective_config.json` 一致。
- [x] 历史27点策略已完成10局成功演练，累计120/120；另有drill10程序异常中止。
- [x] 1100 局 25 点归档保留备查（已被 23 点取代）。
- [ ] 多局真机均全清后冻结代码与配置，再进入正式测试准备。
- [ ] （可选）逼近步长细网格（0.85 → 0.88/0.90），敏感性显示收益在噪声内，非必需。

## 5. 当前默认参数（2026-09-12 定稿）

- **23 点包围网**：`scan_inner_count=8`、`scan_inner_radius_m=980.0`、
  `scan_outer_count=14`、`scan_outer_radius_m=1848.0`。
- **调度**：`task_bias_verify_m=-700.0`（验证任务延后；实测 −500 ~ −2000 m 为平台区，−700 最优）。
- 其余不变：首次推进 300 m、逼近系数 0.85 / 上限 400 m、环形兜底 7 点、清除后复测开。
- `config.py` 默认值与 `configs/baseline.json` 已逐字段一致（`to_dict()` 相等）。
- 候选留档：`configs/optimized22.json`（22 点，更省 0.5%，余量 10.8 m）、
  `configs/optimized23.json`（23 点，即当前默认）。

## 6. 当前风险

- 结论全部来自本地模拟器；真实模拟器的误差模型、接收半径实现是否与题面一致，是主要
  不确定性。若担心接收半径下界实现偏差，用 `configs/conservative.json`（37 点，余量 88.6 m）。
- 23 点包围网仍比问题 3 多较多普查工作量，单案例总时间约 2.0小时（虚拟），仍是
  移动主导，但比问题 3 明显更贵——这是保证定向覆盖的固有成本，不宜为省时间削减。
- 23 点覆盖余量（15.4 m）比原 25 点（21.0 m）更薄。若担心真实模拟器的接收半径实现
  与题面有偏差，可回退 25 点参数或改用 `configs/conservative.json`（37 点，余量 88.6 m）。

## 7. 文件与复现入口

- 演练入口：`scripts/run_drill.py`
- 核心策略：`src/cumcm_b4/strategy.py`
- 覆盖论证：`src/cumcm_b4/coverage.py`
- 总索引：`logs/drill_index.jsonl`
- 自动测试：`python -m unittest discover -s tests -v`

## 8. 2026-09-12 第二轮时间优化（本轮）

### 已完成

- [x] 64 项自动测试重跑通过（5.7 s），代码无漂移。
- [x] 建立时间画像工具 `scripts/profile_time.py`（按阶段分解虚拟时间与位移）。
      200 案例全清，均值 7716.9 s；扫描网点普查占 **65.0%**、其中纯位移 3203 s。
- [x] 验证扫描路线已最优：25 点包围网贪心路线 17898.8 m，2-opt/Or-opt 无法再缩短。
- [x] 严格布局搜索 `scripts/search_layout_v3.py`（枚举 980 → 粗筛 275 → step=2 精筛 70）
      + `scripts/coverage_refine.py`（step=1 精核）。**发现粗网格低估最坏半径可达 42 m。**
- [x] 配对实验 `scripts/ab_test.py`：新增 `adopt` / `layout2` / `verify_fine` / `fine2` /
      `lateral` / `farskip` / `smart` 等套件，全部同种子配对。
- [x] 产出候选配置 `configs/optimized22.json`（22 点，−6.72%，余量 10.8 m）与
      `configs/optimized23.json`（23 点，−6.21%，余量 15.4 m），均通过 `validate()`。
- [x] 加固 `coverage.py`：热循环 `math.*` 属性查找提升为模块级常量，消除罕见崩溃点；
      重构前后数值逐位一致。
- [x] 更新 `RESULTS.md` 第 6.3（布局再优化）与 6.4（校核工具崩溃与加固）节。

### 决策结果（2026-09-12）

- [x] **用户选定 23 点（均衡）**：已改 `config.py` 默认值 + `configs/baseline.json`，逐字段一致。
- [x] **`task_bias_verify_m` 由 −300 改为 −700**（配对 300 案例全清，再省约 5%）。
- [x] **23 点首局真机（drill12）**：10/10 全清、`all_done`，`effective_config.json` 与默认一致。
- [ ] **继续多局真机**（drill13、drill14…）积累统计；每局需在模拟器点「问题 4 演练测试」开新会话。
- [x] drill10 的 `TypeError` **已结案**：在 300 案例 A/B 中复现于 `geometry.py`
      （`math.dist` 在热循环中被观测为 float），与 6.4 的 coverage 崩溃同源。
      已按同一模式把 `geometry.py` 全部 `math.*` 提为模块级常量，64 项测试全过。

### 淘汰清单（本轮实测，勿再尝试）

| 变体 | 结果 | 原因 |
|---|---|---|
| 扫描点跳过已知频道（lean_all / LeanScan2） | −1.3% 但漏清 | 牺牲冗余示向度，风险不可接受 |
| 最小化扫描点数上限 | +21% | 逆效应 |
| 先扫后定位 / 延后清除 | +14.33% / +3.36% | 明确变慢 |
| TSP 重排清除顺序 | 无收益 | 贪心已最优 |
| 插入绕行代价 / 斜坡偏置 | +3.5% / 不如常数 | 简单规则胜出 |
| 逼近步长上限于 bias−700 下再缩小 | 变慢 | 与 bias−300 下结论相反，已复测 |


---

## 9. 2026-09-12 第三轮：优化空间全面排查 + 激进路线实测

### 已完成

- [x] 真机耗时解析器 `scripts/profile_drill.py`（按每局自己的 `effective_config.json` 重建
      扫描点集合），真机 12 局与离线 300 例三口径互证，画像可信。
- [x] 全面参数排查（各 300 例配对）：`bisect_tolerance_m=30` 是唯一安全收益（−0.28%）；
      清除余量、侧向验证、逼近步长均无收益；早停不可行（源总数不固定）。
- [x] **修正一条长期误判**：旧实验中 lateral −3.63% / s150 −2.69% 是在旧基线
      （25 点 + bias−300）下测得，在当前 23 点 + bias−700 下**全部归零**——已被 v700 吸收。
      以后引用 `outputs/ab_*.txt` 收益前必须先核对基线配置。
- [x] 激进路线（19 点稀疏网）大样本实测：1200 例 **1198/1200，−5.76%**；
      累计 1800 例漏 3 例 = **0.167%/局**。
- [x] 两次消除风险的尝试均失败并留档：盲区补点（盲区是整圈）、自由布局退火（无改善）。
- [x] 安全阵营复查：22 点在安全线内但巡回并不更短，23 点仍最优。
- [x] 落地：`config.py` 新增 `scan_extra_points` 与 `allow_unsafe_coverage`（默认 False，
      `validate()` 会拒绝覆盖不足布局）；新增 `configs/layout_p19_aggressive.json` 与
      `configs/layout_p23_safe.json`。代码默认仍是安全 23 点。
- [x] 记录已写入 `RESULTS.md`：第 8 节（耗时画像 / 轨迹可视化 / 交错路线否决）、第 9 节（激进路线，含 9.4 与题目硬约束的冲突分析）。

### 待办

- [ ] 用激进配置跑一局真机（drill13）拿真实数据：演练可用激进，正式测试建议用 23 点。
- [ ] 决定是否采纳 `bisect_tolerance_m` 12 → 30（安全，−0.28%）。

### 本轮淘汰清单（已实测，勿再尝试）

| 方向 | 结果 | 原因 |
|---|---|---|
| 均匀网上补自由点填盲区 | 无效 | 19 点盲区是整圈（206 个坏采样点，r 840–1800 m 全角度），非局部扇区 |
| 19 点自由布局退火 | 未找到可行解 | 均匀起点 worst 1198 无改善；随机起点 inf 跑飞 |
| 减到 22 点 | 无收益 | 安全线内但巡回 17799–18096 m，不比 23 点的 17841 m 短 |
| 内外环交错巡回 | 无效 | 2-opt 会把所有交错顺序还原成同一解（详见 RESULTS 8.3） |
| 早停收网 | 不可行 | 源总数不固定（10–14），无法判定已清完 |
| `clear_safety_margin_m` 调大 | 全面变慢 | 当前 m2 即最优 |
| 侧向验证 / 逼近步长缩小 | 无收益 | 已被 v700 吸收 |
